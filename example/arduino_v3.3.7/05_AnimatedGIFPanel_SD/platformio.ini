[platformio]
; The sketch folder itself is the source dir: PlatformIO only converts .ino
; files found at the top level of src_dir, so each example is its own project.
src_dir = .
extra_configs = ../common.ini

[env:esp32s3]
lib_deps =
    ${common.lib_deps}
    bitbank2/AnimatedGIF@^2.1.0
